export default function EntrarNaPartida({ params }) {
  return (
    <main style={{ padding: 40, fontFamily: 'var(--font-geist-sans)' }}>
      <h1>Partida {params.codigo}</h1>
      <p>
        Tela de entrada (código de turma + apelido + sorteio de papel) ainda não
        implementada — é a Fase 3 do roadmap, depois do conteúdo do cenário estar escrito.
      </p>
    </main>
  )
}
